<?php

return [
    // Preset profile-avatar characters a user can pick in Settings → Profile. iOS maps each key
    // to its own image asset (Avatar_<key>) — this list is the shared contract between the two.
    'keys' => ['default', 'nasi', 'roti', 'milo', 'laksa', 'teh_tarik', 'mascot'],
];
