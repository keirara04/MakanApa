# Loaded after Debian's security.conf (alphabetical), so these win over its
# "ServerTokens OS" default. Hides Apache/Debian versions from the Server header
# and error pages.
ServerTokens Prod
ServerSignature Off
TraceEnable Off
