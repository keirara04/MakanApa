; php:8.4-apache ships with upload_max_filesize=2M, well under this app's own
; advertised restaurant_photos.max_size_kb (5MB, config/restaurant_photos.php) — PHP was
; silently rejecting the upload before Laravel's own `image`/`max` validation ever ran,
; surfacing as "The photo failed to upload." post_max_size stays above upload_max_filesize
; to leave room for the multipart request's other fields.
upload_max_filesize = 8M
post_max_size = 10M
